/*
    This file was generated automatically by Alchitry Labs 2.0.52-BETA.
    Do not edit this file directly. Instead edit the original Lucid source.
    This is a temporary file and any changes made to it will be destroyed.
*/

module adder #(
        parameter SIZE = 6'h20
    ) (
        input wire [(SIZE)-1:0] a,
        input wire [(SIZE)-1:0] b,
        input wire alufn0,
        output reg [(SIZE)-1:0] s,
        output reg z,
        output reg v,
        output reg n
    );
    logic [(SIZE)-1:0] xb;
    localparam _MP_SIZE_1870302555 = SIZE;
    logic [(_MP_SIZE_1870302555)-1:0] M_rca_a;
    logic [(_MP_SIZE_1870302555)-1:0] M_rca_b;
    logic M_rca_cin;
    logic M_rca_cout;
    logic [(_MP_SIZE_1870302555)-1:0] M_rca_s;
    
    rca #(
        .SIZE(_MP_SIZE_1870302555)
    ) rca (
        .a(M_rca_a),
        .b(M_rca_b),
        .cin(M_rca_cin),
        .cout(M_rca_cout),
        .s(M_rca_s)
    );
    
    
    always @* begin
        xb = b ^ {SIZE{alufn0}};
        M_rca_cin = alufn0;
        M_rca_a = a;
        M_rca_b = xb;
        s = M_rca_s;
        z = ~(|M_rca_s);
        v = (a[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)] & xb[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)] & !M_rca_s[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)]) | (!a[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)] & !xb[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)] & M_rca_s[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)]);
        n = M_rca_s[(($bits(SIZE) > $bits(1'h1) ? $bits(SIZE) : $bits(1'h1)) + 1)'(SIZE - 1'h1)];
    end
    
    
endmodule